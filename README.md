Cobra and gorm `TODO` CLI application for training
Tasks :

1. Cobra
   -`add -n` : add new todo
   -`ls ; ls -d (list only done todos)` : list todos
   -`update -i ID -n NewTaskNew` : update todo with ID
   -`delete -i`   : delete todo with ID

2. Gorm/DB
3. Table
